import { TopBar } from "@/components/layout/TopBar";

export default function NewCampaignPage() {
  return (
    <>
      <TopBar title="New campaign" subtitle="Start with the campaign level — ad sets and ads come next" />
      <p className="text-sm text-muted">The campaign form lands in the next build phase.</p>
    </>
  );
}
